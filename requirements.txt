# LaserForge Full Requirements (core + camera).
# For a lighter install without camera support, use only requirements-core.txt.
# Install all: pip install -r requirements.txt

-r requirements-core.txt
-r requirements-camera.txt
